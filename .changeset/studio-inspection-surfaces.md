---
"@anvia/studio": minor
---

Add Studio inspection surfaces for memory, runtime status, richer agent metadata, and direct tool invocation, with in-memory storage as the default and optional SQLite persistence.
