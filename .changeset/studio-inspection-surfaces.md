---
"@anvia/studio": minor
---

Add Studio inspection surfaces for memory, runtime status, richer agent metadata, direct tool invocation, pipeline replay controls, and realtime observability events, with in-memory storage as the default and optional SQLite persistence.
