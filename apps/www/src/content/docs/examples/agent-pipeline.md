---
title: "Agent pipeline"
description: "Use an agent as one step inside a larger pipeline."
section: examples
sidebar:
  group: "Pipelines"
  order: 6
---

## What this example will cover

This placeholder reserves the route and menu entry for a future recipe: use an agent as one step inside a larger pipeline.

## Planned ingredients

The final recipe should cover agent step boundaries, prompt inputs, tool access, downstream outputs.

## Implementation notes

The finished page should include runnable code, required environment variables, expected output, and links to the deeper docs pages that explain the underlying behavior. It should adapt from current repo patterns instead of copying cookbook source text as-is.
